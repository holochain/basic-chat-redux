{
  "name": "basic-chat",
  "version": "0.0.16",
  "dependencies": {
    "@holochain/hc-web-client": "^0.5.0",
    "object-path-immutable": "^1.0.1",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-linkify": "^0.2.2",
    "react-scripts": "next",
    "vuid": "^1.0.0"
  },
  "scripts": {
    "start": "REACT_APP_WEBSOCKET_INTERFACE='ws://localhost:3401' PORT=3001 react-scripts start",
    "build": "rm -rf ../ui && react-scripts build && mv ./build ../ui",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "lint": "standard",
    "clean": "rm -rf ../ui"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "@babel/core": "^7.5.5",
    "babel-loader": "^8.0.5"
  }
}
